<table id="grid-table">
    <tbody>
    {{#each row in gridCells}}
      <tr>
          {{#each cell in row}}
            <td {{bind-attr class="cell.class"}}> </td>
          {{/each}}
      </tr>
    {{/each}}
    </tbody>
</table>